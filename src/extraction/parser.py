"""Parser to extract structured fields from raw text."""

def parse_claim(text: str) -> dict:
    # Very small example parser — replace with real logic
    return {
        'raw_text_preview': text[:200]
    }
