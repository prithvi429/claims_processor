"""Helpers for date/amount normalization and simple PII redaction."""

def normalize_date(dt_str: str) -> str:
    return dt_str

def redact_pii(text: str) -> str:
    # naive redaction
    return text
